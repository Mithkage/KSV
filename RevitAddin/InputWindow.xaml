<Window x:Class="RT_Isolate.InputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Isolate Elements Input" Height="180" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        Background="#FF80CBC4" >
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="3" FontWeight="Bold" Margin="0,0,0,5" Foreground="#FF333333">
            Enter values to isolate elements:
        </TextBlock>
        <TextBlock Grid.Row="1" Grid.ColumnSpan="3" TextWrapping="Wrap" Margin="0,0,0,10" Foreground="#FF333333">
            Enter one or more values, separated by commas, to isolate elements by 'RTS_ID' or 'RTS_Cable_XX' parameters.
            Example: 'C101, C102, Cable-A'
        </TextBlock>

        <TextBox x:Name="InputTextBox" Grid.Row="2" Grid.ColumnSpan="3" MinHeight="25" VerticalContentAlignment="Center"
                 BorderBrush="#FFCC0000" BorderThickness="1"/>
        <StackPanel Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="OK" Width="80" Height="25" Margin="0,0,10,0" Click="Ok_Click"
                    Background="#FFCC0000" Foreground="White" BorderThickness="0"/>
            <Button Content="Cancel" Width="80" Height="25" Click="Cancel_Click"
                    Background="#FFE0E0E0" Foreground="#FF333333" BorderThickness="1" BorderBrush="#FF333333"/>
        </StackPanel>
    </Grid>
</Window>