<!--
//  FILE: ProgressBarWindow.xaml 
//
// Description:
// Defines the user interface for a general-purpose progress bar window. This window provides
// real-time feedback to the user for long-running operations, including a
// progress bar, a status message, an estimated time remaining, and a cancel button.
//
// Author: Kyle Vorster
// Company: ReTick Solutions Pty Ltd
//
// Change Log:
// - August 9, 2025: Removed hardcoded icon path from XAML to rely on dynamic loading from code-behind.
// - August 9, 2025: Updated UI to match the modern style of the Routing Report progress bar.
// - July 31, 2025: Added a TextBlock for room processing status.
// - July 30, 2025: Initial creation of the progress bar UI.
//
-->
<Window x:Class="RTS.UI.ProgressBarWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Processing Elements" Height="220" Width="550"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="True"
        Topmost="True"
        Background="#F5F5F5"
        ResizeMode="CanMinimize"
        WindowStyle="SingleBorderWindow"
        FontFamily="Segoe UI">

    <Window.Resources>
        <!-- Color Palette (matched with RoutingReportProgressBarWindow) -->
        <SolidColorBrush x:Key="PrimaryBrush" Color="#FF26A69A"/>
        <SolidColorBrush x:Key="PrimaryHoverBrush" Color="#FF4DB6AC"/>
        <SolidColorBrush x:Key="PrimaryPressedBrush" Color="#FF00897B"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#FFBDBDBD"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#212121"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#757575"/>
        <SolidColorBrush x:Key="WhiteBrush" Color="White"/>

        <!-- Modern Button Style -->
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource WhiteBrush}" />
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="4" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryHoverBrush}"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryPressedBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Overall Progress Text -->
        <TextBlock Grid.Row="0" x:Name="OverallProgressText" Text="Initializing..." FontWeight="SemiBold" FontSize="16" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,5"/>

        <!-- Progress Bar with Percentage -->
        <Grid Grid.Row="1" Margin="0,5,0,5">
            <ProgressBar x:Name="ProgressBar" Minimum="0" Maximum="100" Height="28" BorderThickness="1" BorderBrush="{StaticResource BorderBrush}"/>
            <TextBlock x:Name="ProgressPercentageText" Text="0%" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        </Grid>

        <!-- Task Description / Time Estimate -->
        <TextBlock Grid.Row="2" x:Name="TaskDescriptionText" Text="Preparing..." FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" FontStyle="Italic" Margin="0,5,0,10"/>

        <!-- Spacer -->
        <Grid Grid.Row="3"/>

        <!-- Cancel Button -->
        <Button Grid.Row="4" x:Name="CancelButton" Content="Cancel" Style="{StaticResource ModernButtonStyle}" HorizontalAlignment="Right" VerticalAlignment="Bottom" Click="CancelButton_Click" Width="100"/>
    </Grid>
</Window>
