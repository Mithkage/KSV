<Window x:Class="RTS.UI.LinkManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Revit Link Manager" Height="722" Width="1597" 
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False"
        Topmost="True"
        Background="#FFE0F2F1"
        MinWidth="900" MinHeight="450">

    <!-- Resource Dictionary for Styles -->
    <Window.Resources>
        <!-- Modern Button Style from your example -->
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FF26A69A"/>
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="BorderBrush" Value="#FF00796B"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#FF616161" Direction="270" ShadowDepth="2" BlurRadius="4" Opacity="0.4"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="8" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF4DB6AC"/>
                    <Setter Property="BorderBrush" Value="#FF00897B"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#FF00897B"/>
                    <Setter Property="BorderBrush" Value="#FF00695C"/>
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="#FF616161" Direction="270" ShadowDepth="1" BlurRadius="2" Opacity="0.6" />
                        </Setter.Value>
                    </Setter>
                </Trigger>
                 <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#FFBDBDBD"/>
                    <Setter Property="Foreground" Value="#FF757575"/>
                    <Setter Property="BorderBrush" Value="#FF9E9E9E"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Style for DataGrid Headers -->
        <Style TargetType="{x:Type DataGridColumnHeader}">
            <Setter Property="Background" Value="#FF00897B"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>
        
        <!-- Style for DataGrid Cells -->
        <Style TargetType="{x:Type DataGridCell}">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#FFB2DFDB"/>
                    <Setter Property="Foreground" Value="Black"/>
                    <Setter Property="BorderBrush" Value="#FF00796B"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <!-- Main Layout Grid -->
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header Text -->
        <TextBlock Grid.Row="0" Text="Project Link Configuration"
                   FontSize="18" FontWeight="SemiBold" Margin="0,0,0,20"
                   HorizontalAlignment="Center" Foreground="#FF333333"/>

        <!-- DataGrid to display links -->
        <DataGrid Grid.Row="1" x:Name="LinksDataGrid" 
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserSortColumns="True"
                  ItemsSource="{Binding Links}"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  HorizontalScrollBarVisibility="Auto"
                  VerticalScrollBarVisibility="Auto"
                  BorderBrush="#FF00796B"
                  BorderThickness="1"
                  SelectionMode="Extended"
                  PreviewKeyDown="LinksDataGrid_PreviewKeyDown">
            <DataGrid.Columns>
                <!-- Link Name (Read-only for real links, editable for placeholders) -->
                <DataGridTextColumn Header="Link File Name" Binding="{Binding LinkName, UpdateSourceTrigger=PropertyChanged}" Width="3*"/>

                <!-- Link Description (Text field) -->
                <DataGridTextColumn Header="Link Description" Binding="{Binding LinkDescription, UpdateSourceTrigger=PropertyChanged}" Width="1.5*"/>
                
                <!-- Last Modified Date (Read-only) -->
                <DataGridTextColumn Header="Last Modified" Binding="{Binding LastModified}" IsReadOnly="True" Width="1.2*"/>

                <!-- Discipline (Dropdown) -->
                <DataGridTemplateColumn Header="Discipline" SortMemberPath="SelectedDiscipline" Width="1*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ComboBox ItemsSource="{Binding AvailableDisciplines}" 
                                      SelectedItem="{Binding SelectedDiscipline, UpdateSourceTrigger=PropertyChanged}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- Coordinates (Dropdown) -->
                <DataGridTemplateColumn Header="Coordinates" SortMemberPath="SelectedCoordinates" Width="1.1*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ComboBox ItemsSource="{Binding AvailableCoordinates}" 
                                      SelectedItem="{Binding SelectedCoordinates, UpdateSourceTrigger=PropertyChanged}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- NEW: Company Name (Text field) -->
                <DataGridTextColumn Header="Company Name" Binding="{Binding CompanyName, UpdateSourceTrigger=PropertyChanged}" Width="1.2*"/>

                <!-- Responsible Person (Text field) -->
                <DataGridTextColumn Header="Responsible Person" Binding="{Binding ResponsiblePerson, UpdateSourceTrigger=PropertyChanged}" Width="1.2*"/>

                <!-- Contact Details (Text field) -->
                <DataGridTextColumn Header="Contact Details" Binding="{Binding ContactDetails, UpdateSourceTrigger=PropertyChanged}" Width="1.2*"/>
                
                <!-- Comments (Text field) -->
                <DataGridTextColumn Header="Comments" Binding="{Binding Comments, UpdateSourceTrigger=PropertyChanged}" Width="2*"/>

            </DataGrid.Columns>
        </DataGrid>

        <!-- Action Buttons at the bottom -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Add Placeholder" x:Name="AddPlaceholderButton" Click="AddPlaceholderButton_Click" Style="{StaticResource ModernButtonStyle}" Margin="0,0,10,0" Width="123"/>
            <Button Content="Save Profile" x:Name="SaveButton" Click="SaveButton_Click" Style="{StaticResource ModernButtonStyle}" Margin="0,0,10,0" Width="91"/>
            <Button Content="Close" x:Name="CloseButton" Click="CloseButton_Click" Style="{StaticResource ModernButtonStyle}" Width="56"/>
        </StackPanel>
    </Grid>
</Window>
