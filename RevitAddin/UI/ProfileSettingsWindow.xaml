<!--
    File: ProfileSettingsWindow.xaml
    Description: Defines the UI for the Profile Settings window. Users can map model
                 element categories (like Grids, Walls, etc.) to specific Revit links.
-->
<Window x:Class="RTS.UI.ProfileSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RTS.UI"
        mc:Ignorable="d"
        Title="Profile Settings" Height="450" Width="800"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Topmost="True"
        Background="#FFE0F2F1"
        ResizeMode="NoResize">

    <!-- Reusing the same styles from the main Link Hub window -->
    <Window.Resources>
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FF26A69A"/>
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="BorderBrush" Value="#FF00796B"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="8" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF4DB6AC"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#FF00897B"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ColumnHeaderFilterStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridColumnHeader">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ContentPresenter Grid.Column="0" Content="{TemplateBinding Content}" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="5,0"/>
                            <Button Grid.Column="1"
                                    Width="18" Height="18"
                                    Margin="2,0,2,0"
                                    Click="ColumnHeaderFilter_Click"
                                    Tag="{TemplateBinding Content}">
                                <Path Data="M0,0 L4,4 L8,0 Z" Fill="Gray"/>
                            </Button>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Tab Control -->
        <TabControl Grid.Row="0">
            <TabItem Header="Model Categories">
                <StackPanel>
                    <StackPanel>
                        <DataGrid ItemsSource="{Binding ModelCategoryMappings}"
                                  AutoGenerateColumns="False"
                                  HeadersVisibility="Column"
                                  CanUserAddRows="False"
                                  CanUserResizeColumns="True"
                                  Margin="0,10,0,0"
                                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                                  ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                  Height="300">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Model Category"
                                                    Binding="{Binding CategoryName}"
                                                    IsReadOnly="True"
                                                    Width="*"
                                                    HeaderStyle="{StaticResource ColumnHeaderFilterStyle}"/>
                                <DataGridTemplateColumn Header="Responsible"
                                                        Width="2*"
                                                        HeaderStyle="{StaticResource ColumnHeaderFilterStyle}">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <ComboBox ItemsSource="{Binding DataContext.AvailableLinks, RelativeSource={RelativeSource AncestorType=Window}}"
                                                      SelectedItem="{Binding SelectedLink, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </StackPanel>
            </TabItem>

            <TabItem Header="Annotation Categories">
                <DataGrid ItemsSource="{Binding AnnotationCategoryMappings}" AutoGenerateColumns="False" HeadersVisibility="Column" CanUserAddRows="False" CanUserResizeColumns="True" Margin="0,10,0,0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Annotation Category"
                                            Binding="{Binding CategoryName}"
                                            IsReadOnly="True"
                                            Width="*"
                                            HeaderStyle="{StaticResource ColumnHeaderFilterStyle}"/>
                        <DataGridTemplateColumn Header="Responsible"
                                                Width="2*"
                                                HeaderStyle="{StaticResource ColumnHeaderFilterStyle}">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <ComboBox ItemsSource="{Binding DataContext.AvailableLinks, RelativeSource={RelativeSource AncestorType=Window}}"
                                              SelectedItem="{Binding SelectedLink, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <TabItem Header="Coordinate Systems">
                <DataGrid ItemsSource="{Binding CoordinateSystemMappings}" AutoGenerateColumns="False" HeadersVisibility="Column" CanUserAddRows="False" CanUserResizeColumns="True" Margin="0,10,0,0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Coordinate System"
                                            Binding="{Binding SystemName}"
                                            IsReadOnly="True"
                                            Width="*"
                                            HeaderStyle="{StaticResource ColumnHeaderFilterStyle}"/>
                        <DataGridTemplateColumn Header="Responsible"
                                                Width="2*"
                                                HeaderStyle="{StaticResource ColumnHeaderFilterStyle}">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <ComboBox ItemsSource="{Binding DataContext.AvailableLinks, RelativeSource={RelativeSource AncestorType=Window}}"
                                              SelectedItem="{Binding SelectedLink, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Save" Style="{StaticResource ModernButtonStyle}" Margin="0,0,10,0" IsDefault="True" Click="SaveButton_Click" Width="47"/>
            <Button Content="Clear Profile" Style="{StaticResource ModernButtonStyle}" Margin="0,0,10,0" Click="ClearProfileButton_Click" Width="86"/>
            <Button Content="Cancel" Style="{StaticResource ModernButtonStyle}" Margin="0,0,0,0" IsCancel="True" Width="58"/>
        </StackPanel>
    </Grid>
</Window>
