<Window x:Class="RTS.UI.InitiateParametersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="RTS - Initiate Shared Parameters" Height="320" Width="450"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False"
        Topmost="True"
        Background="#FFE0F2F1"
        ResizeMode="NoResize">

    <!-- Resource Dictionary for Styles -->
    <Window.Resources>
        <!-- Modern Button Style from your example -->
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FF26A69A"/>
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="BorderBrush" Value="#FF00796B"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#FF616161" Direction="270" ShadowDepth="2" BlurRadius="4" Opacity="0.4"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="8" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF4DB6AC"/>
                    <Setter Property="BorderBrush" Value="#FF00897B"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#FF00897B"/>
                    <Setter Property="BorderBrush" Value="#FF00695C"/>
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="#FF616161" Direction="270" ShadowDepth="1" BlurRadius="2" Opacity="0.6" />
                        </Setter.Value>
                    </Setter>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#FFBDBDBD"/>
                    <Setter Property="Foreground" Value="#FF757575"/>
                    <Setter Property="BorderBrush" Value="#FF9E9E9E"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Style for CheckBoxes -->
        <Style TargetType="CheckBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#FF333333"/>
            <Setter Property="Margin" Value="0,8"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <!-- Main Layout Grid -->
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header Text -->
        <TextBlock Grid.Row="0" Text="Select Parameter Groups to Initiate"
                   FontSize="18" FontWeight="SemiBold" Margin="0,0,0,20"
                   HorizontalAlignment="Center" Foreground="#FF333333" TextWrapping="Wrap" TextAlignment="Center"/>

        <!-- Parameter Selection Checkboxes -->
        <StackPanel Grid.Row="1" VerticalAlignment="Center">
            <CheckBox x:Name="DetailItemsCheckBox" Content="Detail Item Parameters (PC_)" IsChecked="True"/>
            <CheckBox x:Name="CableConduitCheckBox" Content="Cable/Conduit Parameters (RTS_)" IsChecked="True"/>
            <CheckBox x:Name="RtsIdCheckBox" Content="RTS_ID Parameter" IsChecked="True"/>
            <CheckBox x:Name="WiresCheckBox" Content="Wire Parameters (PC_)" IsChecked="True"/>
        </StackPanel>

        <!-- Action Buttons at the bottom -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Initiate" x:Name="InitiateButton" Click="InitiateButton_Click" Style="{StaticResource ModernButtonStyle}" Margin="0,0,10,0" Width="90"/>
            <Button Content="Close" x:Name="CloseButton" IsCancel="True" Style="{StaticResource ModernButtonStyle}" Width="70"/>
        </StackPanel>
    </Grid>
</Window>
