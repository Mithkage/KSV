<!--
//  FILE: ProgressBarWindow.xaml 
//
// Description:
// Defines the user interface for a progress bar window. This window provides
// real-time feedback to the user for long-running operations, including a
// progress bar, a status message, an estimated time remaining, and a cancel button.
//
// Author: Kyle Vorster
// Company: ReTick Solutions Pty Ltd
//
// Change Log:
// - July 31, 2025: Added a TextBlock for room processing status.
// - July 30, 2025: Initial creation of the progress bar UI.
//
-->
<Window x:Class="RTS.UI.ProgressBarWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Processing Elements" Height="220" Width="500"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Topmost="True"
        Background="#FFE0F2F1"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow">

    <Window.Resources>
        <!-- Modern Button Style (copied from LinkManagerWindow for consistency) -->
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FF26A69A"/>
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="BorderBrush" Value="#FF00796B"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="8" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF4DB6AC"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#FF00897B"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Status Message TextBlock -->
        <TextBlock Grid.Row="0" x:Name="StatusText" Text="Initializing..." FontSize="14" Margin="0,0,0,10" HorizontalAlignment="Left"/>

        <!-- Progress Bar -->
        <ProgressBar Grid.Row="1" x:Name="ProgressBar" Minimum="0" Maximum="100" Height="25"/>

        <!-- Time Estimate TextBlock -->
        <TextBlock Grid.Row="2" x:Name="TimeEstimateText" Text="Calculating time remaining..." FontSize="12" Margin="0,10,0,0" HorizontalAlignment="Left" FontStyle="Italic"/>

        <!-- Room Status TextBlock -->
        <TextBlock Grid.Row="3" x:Name="RoomStatusText" Text="" FontSize="12" Margin="0,5,0,0" HorizontalAlignment="Left" FontStyle="Italic"/>

        <!-- Cancel Button -->
        <Button Grid.Row="4" x:Name="CancelButton" Content="Cancel" Style="{StaticResource ModernButtonStyle}" HorizontalAlignment="Right" VerticalAlignment="Bottom" Click="CancelButton_Click"/>
    </Grid>
</Window>
